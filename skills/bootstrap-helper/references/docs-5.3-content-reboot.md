### Example Link (HTML)

Source: https://getbootstrap.com/docs/5.3/content/reboot

Shows a standard anchor link styled by Bootstrap. Links have a default color and underline, with hover states. Placeholder links without an href are styled differently.

```html
<a href="#">This is an example link</a>

```

--------------------------------

### Example Paragraph (HTML)

Source: https://getbootstrap.com/docs/5.3/content/reboot

Demonstrates a basic paragraph element styled by Bootstrap. Paragraphs have their top margin removed and a bottom margin of 1rem for consistent spacing.

```html
<p>This is an example paragraph.</p>

```

--------------------------------

### Indicating Sample Output with `<samp>`

Source: https://getbootstrap.com/docs/5.3/content/reboot

Demonstrates the use of the `<samp>` tag for displaying sample output from a computer program. This tag helps distinguish program output from other text.

```html
<samp>This text is meant to be treated as sample output from a computer program.</samp>
```

--------------------------------

### HTML Example of Hidden Input Element

Source: https://getbootstrap.com/docs/5.3/content/reboot

Demonstrates the usage of the HTML5 [hidden] attribute on an input element. This attribute is styled by Bootstrap to hide the element.

```html
<input type="text" hidden>

```

--------------------------------

### Styling Tables with Bootstrap

Source: https://getbootstrap.com/docs/5.3/content/reboot

Provides an example of an HTML table styled with Bootstrap. It includes a caption and demonstrates the basic structure for table headings and cells. Bootstrap's `.table` class offers further styling options.

```html
<table>
  <caption>
    This is an example table, and this is its caption to describe the contents.
  </caption>
  <thead>
    <tr>
      <th>Table heading</th>
      <th>Table heading</th>
      <th>Table heading</th>
      <th>Table heading</th>
    </tr>
  </thead>
  <tbody>
    <tr>
      <td>Table cell</td>
      <td>Table cell</td>
      <td>Table cell</td>
      <td>Table cell</td>
    </tr>
    <tr>
      <td>Table cell</td>
      <td>Table cell</td>
      <td>Table cell</td>
      <td>Table cell</td>
    </tr>
    <tr>
      <td>Table cell</td>
      <td>Table cell</td>
      <td>Table cell</td>
      <td>Table cell</td>
    </tr>
  </tbody>
</table>
```

--------------------------------

### Horizontal Rules (HTML)

Source: https://getbootstrap.com/docs/5.3/content/reboot

Provides examples of Bootstrap's horizontal rule (`<hr>`) element. HRs are styled with a default opacity and inherit their color. They can be modified using utility classes for borders and opacity.

```html
<hr>

<div class="text-success">
  <hr>
</div>

<hr class="border border-danger border-2 opacity-50">
<hr class="border border-primary border-3 opacity-75">

```

--------------------------------

### Indicating User Input with `<kbd>`

Source: https://getbootstrap.com/docs/5.3/content/reboot

Illustrates the use of the `<kbd>` tag for marking keyboard input. This is useful for showing commands, shortcuts, or any text entered by a user via the keyboard.

```html
To switch directories, type <kbd>cd</kbd> followed by the name of the directory.<br>
To edit settings, press <kbd><kbd>Ctrl</kbd> + <kbd>,</kbd></kbd>
```

--------------------------------

### Displaying Code Blocks with `<pre>`

Source: https://getbootstrap.com/docs/5.3/content/reboot

Demonstrates the use of the `<pre>` tag for displaying multiple lines of code. It highlights the need to escape angle brackets within the code for correct rendering. The `<pre>` element's margins are adjusted for better spacing.

```html
<p>Sample text here...</p>
<p>And another line of sample text here...</p>
```

```html
<pre><code>&lt;p&gt;Sample text here...&lt;/p&gt;
&lt;p&gt;And another line of sample text here...&lt;/p&gt;
</code></pre>
```

--------------------------------

### Indicating Variables with `<var>`

Source: https://getbootstrap.com/docs/5.3/content/reboot

Shows how to use the `<var>` tag to denote variables in mathematical expressions or code. This helps in semantically marking up variable names.

```html
<var>y</var> = <var>m</var><var>x</var> + <var>b</var>
```

--------------------------------
